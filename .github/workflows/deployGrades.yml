name: Deploy Grade Management Services to AWS

on:
  push:
    branches:
      - test
    paths:
      - 'BACKEND/grade_management/**'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 🚀 Checkout del código
      uses: actions/checkout@v3

    - name: 🔑 Acceder al servidor EC2 y desplegar
      uses: appleboy/ssh-action@v0.1.8
      with:
        host: ${{ secrets.EC2_HOST_GRADES }}
        username: ec2-user
        key: ${{ secrets.EC2_SSH_KEY_GRADES }}
        port: 22
        script: |
          echo "✅ Conexión establecida con EC2"

          echo "🛠️ Verificando si Docker y Docker Compose están instalados..."
          if ! command -v docker &> /dev/null; then
            echo "🔧 Instalando Docker..."
            sudo dnf install -y docker
            sudo systemctl start docker
            sudo systemctl enable docker
            sudo usermod -aG docker ec2-user
          fi

          if ! command -v docker-compose &> /dev/null; then
            echo "🔧 Instalando Docker Compose..."
            sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
            sudo chmod +x /usr/local/bin/docker-compose
          fi

          echo "🔑 Iniciando sesión en Docker Hub..."
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

          echo "🛑 Eliminando contenedores e imágenes en conflicto..."
          docker stop $(docker ps -aq) || true
          docker rm $(docker ps -aq) || true
          docker rmi -f ${{ secrets.DOCKER_USERNAME }}/created_grades:latest || true
          docker rmi -f ${{ secrets.DOCKER_USERNAME }}/delete_grades:latest || true
          docker rmi -f ${{ secrets.DOCKER_USERNAME }}/read_grades:latest || true
          docker rmi -f ${{ secrets.DOCKER_USERNAME }}/update_grades:latest || true
          docker network prune -f || true
          docker volume prune -f || true

          echo "📦 Moviéndonos al directorio del proyecto..."
          cd ~/appGrades/BACKEND/grade_management || mkdir -p ~/appGrades/BACKEND/grade_management && cd ~/appGrades/BACKEND/grade_management

          echo "📂 Creando archivo .env correctamente..."
          cat <<EOF > .env
          DB_HOST=${{ secrets.DB_HOST_GRADES }}
          DB_PORT=5432
          DB_USER=${{ secrets.DB_USER_GRADES }}
          DB_PASSWORD=${{ secrets.DB_PASSWORD_GRADES }}
          DB_NAME=${{ secrets.DB_NAME_GRADES }}
          DB_SSL=false
          EOF

          echo "✅ Archivo .env creado con éxito:"
          cat ~/appGrades/BACKEND/grade_management/.env

          echo "⬇️ Creando nuevo docker-compose.yml..."
          cat <<EOF > docker-compose.yml
          version: '3.8'
          services:
            created_grades:
              image: ${{ secrets.DOCKER_USERNAME }}/created_grades:latest
              container_name: created_grades
              restart: always
              environment:
                - APP_PORT=2000
              ports:
                - "2000:2000"
              env_file:
                - .env

            delete_grades:
              image: ${{ secrets.DOCKER_USERNAME }}/delete_grades:latest
              container_name: delete_grades
              restart: always
              environment:
                - APP_PORT=2003
              ports:
                - "2003:2003"
              env_file:
                - .env

            read_grades:
              image: ${{ secrets.DOCKER_USERNAME }}/read_grades:latest
              container_name: read_grades
              restart: always
              environment:
                - APP_PORT=2001
              ports:
                - "2001:2001"
              env_file:
                - .env

            update_grades:
              image: ${{ secrets.DOCKER_USERNAME }}/update_grades:latest
              container_name: update_grades
              restart: always
              environment:
                - APP_PORT=2002
              ports:
                - "2002:2002"
              env_file:
                - .env
          EOF

          echo "✅ Archivo docker-compose.yml creado con éxito."
          cat ~/appGrades/BACKEND/grade_management/docker-compose.yml

          echo "📥 Descargando imágenes de Docker..."
          docker pull ${{ secrets.DOCKER_USERNAME }}/created_grades:latest
          docker pull ${{ secrets.DOCKER_USERNAME }}/delete_grades:latest
          docker pull ${{ secrets.DOCKER_USERNAME }}/read_grades:latest
          docker pull ${{ secrets.DOCKER_USERNAME }}/update_grades:latest

          echo "🚀 Desplegando servicios con Docker Compose..."
          docker-compose up -d --force-recreate --remove-orphans

          echo "🔍 Verificando contenedores en ejecución..."
          docker ps
